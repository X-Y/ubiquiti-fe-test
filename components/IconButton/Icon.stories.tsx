import IconButton from "./IconButton";

const meta = {
  component: IconButton,
  title: "IconButton",
  args: {},
};

export default meta;

export const Basic = {
  args: {
    icon: "list",
  },
};
